import { InsuranceIdValidatorPipe } from './insurance-id-validator.pipe';

describe('InsuranceIdValidatorPipe', () => {
  it('should be defined', () => {
    expect(new InsuranceIdValidatorPipe()).toBeDefined();
  });
});
